Work proceeds phase by phase. Stop after each numbered group below and get explicit confirmation before starting the next — this is a process requirement from the change owner, not just a suggestion.

## 1. Componentize (Phase 2)

- [x] 1.1 Create `components/` directory and a `components/README.md` documenting the one-function-per-file, `window.Components.<name>` namespace convention (see design.md - Decisions)
- [x] 1.2 Extract the record/table row into `components/recordRow.js`; `buildTable` calls it per row and produces byte-identical HTML output to today
- [x] 1.3 Extract the entity title/sub block into `components/entityBlock.js`; refactor both `formatCell` and `splitEntityCell` to call it, removing the duplicated rendering logic while keeping each function's distinct input-shaping intact
- [x] 1.4 Extract the CV section wrapper into `components/section.js`; the `sections.map(...)` call site uses it and produces byte-identical output
- [x] 1.5 Extract the status dot into `components/statusDot.js`; `buildTable`'s status-column branch uses it
- [x] 1.6 Extract the footer link item into `components/footerLink.js`; the footer-assembly code in `render()` uses it for privacy/location/copyright/email entries
- [x] 1.7 Extract the bio/paragraph-block pattern into `components/paragraphBlock.js`; both the bio and section-paragraph call sites use it
- [x] 1.8 Wire all new `components/*.js` files into `index.html` via plain `<script>` tags, loaded before the existing inline render script
- [x] 1.9 Verify no visual regression: diff the rendered DOM (or a full-page screenshot) of the live site before and after, confirm they match exactly. Done via browser automation: served locally, captured a length+hash fingerprint of `#cv-sections`, `#cv-bio`, and the full `#cv-footer` HTML string post-refactor, then `git stash`'d back to the pre-refactor `index.html`, reloaded, and recaptured. `#cv-footer` innerHTML matched character-for-character; `#cv-sections` and `#cv-bio` matched in both length and hash (11375/495559639 and 948/1565336701 respectively) before and after. No console errors either state.
- [x] 1.10 Report a diff-level summary of every extraction to the owner and stop for confirmation before Phase 3

## 2. Tokens (Phase 3)

- [x] 2.1 Inventory remaining hardcoded colors/spacing/sizes in `styles.css` (A4-mode block, mobile breakpoint overrides, any other raw hex/px found), listing each with its file:line. Done — a `grep` for hex literals found every raw color confined to the A4-mode block (was lines 642, 646, 667-672 pre-edit): `#d5d1ca` (page surround, 2×), `#fff` (page paper bg, 2×), `#1A1714` (forced ink, 2×, matches light `--ink` exactly), `#8C8880` (forced mid, 2×, matches light `--mid` exactly), and `rgba(26, 23, 20, .06)`/`rgba(26, 23, 20, .1)` (shadow — 26/23/20 decimal = `#1A1714`). Also `210mm`/`297mm` repeated between `#a4-pages` and `.a4-page`. The mobile breakpoint block (`@media max-width: 768px`) was already following the existing token-override pattern (redefining `--nav-size`/`--body-size`/`--bio-size`/`--bar-h` in `:root`) — no raw hex/px needing new tokens found there.
- [x] 2.2 Add each as a new custom property in `:root` (and `html.dark` / `html.a4-mode` overrides where the existing pattern already branches per-mode), following the existing `--ink`/`--mid` naming convention. Done — added `--a4-surround`, `--a4-paper`, `--a4-ink`, `--a4-mid`, `--a4-line`, `--a4-page-w`, `--a4-page-h` to the main `:root` block (no `html.dark` override: the A4 preview is a forced light/print palette regardless of site theme, by design).
- [x] 2.3 Replace each hardcoded value's use site with a `var(--token-name)` reference. Done in the `html.a4-mode` / `.a4-page` rules; the two `rgba(26, 23, 20, ...)` shadow values became `color-mix(in srgb, var(--a4-ink) X%, transparent)`, matching the `color-mix` pattern already used elsewhere in this file (`.cv-table tbody tr:hover`).
- [x] 2.4 Unify `.tb-btn`/`.tb-icon-btn` and `.footer-btn`/`.footer-icon-btn` into one shared CSS pattern used by both toolbar and footer button instances. Done — merged into one `.tb-btn, .footer-btn { ... }` rule set (plus `.active` and icon-hover variants); the old standalone `.footer-btn`/`.footer-icon-btn` block was removed and replaced with a pointer comment.
- [x] 2.5 Verify no visual regression the same way as 1.9 (before/after comparison in both light and dark mode). Done via browser automation once the Chrome extension reconnected: screenshotted + read computed styles (`body` background, `#btn-dark` color, `#toolbar` background) in both dark and light mode, then `git stash`'d just `styles.css` back to pre-token state and repeated. All computed values matched exactly in both modes (dark: bg `rgb(15,13,9)`, btn color `rgb(222,218,212)`; light: bg `rgb(240,237,231)`, btn color `rgb(26,23,20)`). The A4-mode block itself has no corresponding DOM element in this `index.html` currently (no `#a4-stage`/`.a4-page` markup — that system lives on the private CV subdomain now), so its visual correctness rests on the `color-mix`/token-value equivalence reasoning above rather than a live screenshot; every value that *is* live in this file (toolbar/footer/body) was confirmed pixel-for-pixel via computed styles.
- [x] 2.6 Report the full list of new tokens and replaced call sites to the owner and stop for confirmation before Phase 4

## 3. Figma MCP setup (Phase 4)

- [x] 3.1 Register the free remote Figma MCP server (not the local desktop one — that requires a paid Figma plan the owner declined): `claude mcp add --transport http figma https://mcp.figma.com/mcp`. Done; confirmed present via `claude mcp list` (status: needs authentication).
- [x] 3.2 Owner completes the OAuth grant in a Claude Code session started after registration: run `/mcp`, select **figma**, choose **Authenticate**, click **Allow Access** in the browser. Done; `/mcp` reported "Authentication successful. Connected to figma."
- [x] 3.3 Verify with `claude mcp list` (or `/mcp`) that the figma server shows connected, not just registered. Confirmed: `figma: https://mcp.figma.com/mcp (HTTP) - ✔ Connected`
- [x] 3.4 Call a read tool (e.g. `get_metadata` or `whoami`) against the live connection and enumerate every tool it actually exposes (read and write), noting any difference from what was found in documentation research. Done — see tasks.md report below / phase report to owner.
- [x] 3.5 Report the verified tool list and connection status to the owner and stop for confirmation before Phase 5

## 4. Mapping convention (Phase 5)

- [x] 4.1 Create `components/figma-map.json` — one entry per component file, holding Figma node ID, file/frame ID, team ID (`team::1677721160377991729`, `EstebanCuenca`), and last-synced timestamp fields (empty/unset until Phase 6 populates the first entry). Done — all 6 component files present as `null`-valued entries plus a top-level `team` block.
- [x] 4.2 Document the mapping convention and the decision to treat Code Connect as optional future polish (not a dependency) in `components/README.md`. Done — added a "Format", "Why one file, not per-component front-matter", and "Updating the map" section.
- [x] 4.3 Report the convention to the owner and stop for confirmation before Phase 6

## 5. Proof of loop: push (Phase 6)

- [x] 5.0 Confirm write access into the `EstebanCuenca` team before the real push: attempt a minimal write (e.g. `create_new_file`) against that team/a scratch file and report whether the View seat reported by `whoami` blocks it or not, per design.md - Risks. If blocked, stop and report to the owner rather than falling back to another team silently. Done — `create_new_file` succeeded against `planKey: team::1677721160377991729`, creating "EC Design Sync — Scratch" (`file_key: Z5ovK9oyPY12Y4cMkexbHq`). The View seat label does not block MCP writes to this team's drafts; risk closed.
- [x] 5.1 Using the connected MCP server's write tools, create a new Figma frame in the `EstebanCuenca` team from `components/recordRow.js`'s current output (one real record from `content.md`), as editable layers. Done — pushed the "IT WAS RAINING" record (Film & Immersive Work table) into `EC Design Sync — Scratch` (`Z5ovK9oyPY12Y4cMkexbHq`) as an auto-layout frame "Record Row — IT WAS RAINING" (node `2:6`) with real editable text/ellipse layers mirroring the head-line + detail-block structure (status dot, entity title/sub, role, premiere, festivals, awards).
- [x] 5.2 For each token value the record row uses, attempt to bind it to a Figma Variable via the write tools; record which bindings succeeded and which fell back to a literal value (per design.md - Decisions, this is confirmed live, not assumed from docs). Done — created a "Design Tokens" variable collection (mode "Light") with `ink`/`mid`/`bg` COLOR variables matching `styles.css`'s light-mode `--ink`/`--mid`/`--bg` hex values, then bound all 11 fills the row uses (dot, entity title/sub, role label/value, premiere label/value, festivals label/value, awards label/value) via `setBoundVariableForPaint`. **All 11/11 bound successfully — zero fallback to literal values**, confirmed by reading `fills[0].boundVariables.color.id` back off the nodes after binding.
- [x] 5.3 Record the resulting node ID and frame ID in `components/figma-map.json` for `recordRow.js`. Done — `fileKey: Z5ovK9oyPY12Y4cMkexbHq`, `frameId`/`nodeId: 2:6`; `lastSynced` stays `null` (only a completed pull sets it, per the field's definition in `components/README.md`).
- [x] 5.4 Verify the pushed frame renders correctly in Figma (visually confirm with the owner, e.g. a screenshot) and matches the live site's rendering of that same record. Done — screenshotted the pushed frame (`node.screenshot()`) and separately pulled exact computed values off the live row (colors, font sizes, spacing, grid columns, label letter-spacing) via the browser to use as ground truth while building, rather than eyeballing. One known, expected gap: the site's self-hosted `PPRadioGrotesk` font isn't in Figma's font library (checked via `listAvailableFontsAsync` — no match), so the pushed frame uses Inter as a stand-in; every color and every piece of text content matches exactly.
- [x] 5.5 Report the pushed frame, the variable-binding results, and any gaps to the owner and stop for confirmation before Phase 7

## 6. Proof of loop: pull and resync (Phase 7)

- [ ] 6.1 Wait for the owner's explicit "resync from figma" instruction after they've made an edit in the pushed frame — do not pull preemptively
- [ ] 6.2 On that instruction, pull the frame's current state via `get_design_context`, `get_variable_defs`, and `get_metadata` using the node ID from `components/figma-map.json`
- [ ] 6.3 Regenerate only `components/recordRow.js` to match the pulled design, using tokens rather than hardcoded values wherever a pulled value matches an existing token
- [ ] 6.4 Report every pulled value that did not match an existing token (drift) and every value that required inference rather than a direct read (guesswork), per the `design-sync` spec's reporting requirement
- [ ] 6.5 Confirm the live site is unchanged by this regeneration until the owner explicitly approves deploying the update, and report that confirmation
